@* 
   Arquivo de inicialização das views.
   Este código é executado antes da renderização de cada view dentro desta pasta (e de suas subpastas).
*@
@{
    // Define o layout padrão que será aplicado a todas as views desta área, 
    // a menos que a view sobrescreva essa definição.
    Layout = "_Layout";
}