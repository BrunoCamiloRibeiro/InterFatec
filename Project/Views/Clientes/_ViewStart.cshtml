@* O arquivo _ViewStart.cshtml é executado antes de cada View desta pasta. 
   Ele define o Layout padrão (neste caso, "_Layout") para não precisarmos repeti-lo em todas as telas. *@
@{
    Layout = "_Layout";
}