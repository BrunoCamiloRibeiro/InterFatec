@*
    Este arquivo define o layout raiz compartilhado pelas Views da pasta de Produtos.
    Garante que os menus laterais/superiores da aplicação (presentes no _Layout.cshtml) sejam carregados.
*@
@{
    // Atribui "_Layout" à propriedade de Layout padrão, poupando a repetição em cada view individual.
    Layout = "_Layout";
}