@model IEnumerable<AgendamentoListagemViewModel>

<h1 class="h1 text-center mx-3">Agendamentos</h1>

<p class="container-fluid row justify-content-end">
	<a class="btn btn-primary w-25" asp-action="Criar">Novo agendamento</a>
</p>

@if (Model != null && Model.Any())
{
	<table class="table table-striped" border="1">
		<thead>
			<tr>
				<th>Número</th>
				<th>Cliente</th>
				<th>Data e Hora</th>
				<th>Status</th>
				<th>Total</th>
				<th>Serviços</th>
				<th>Produtos</th>
				<th>Ações</th>
			</tr>
		</thead>
		<tbody>
		@foreach (var item in Model)
		{
			<tr>
				<td>@item.Nr</td>
				<td>@item.ClienteNome</td>
				<td>@item.DataHora</td>
				<td>@item.Status</td>
				<td>@item.Total</td>
				<td>@item.QuantidadeServicos</td>
				<td>@item.QuantidadeProdutos</td>
				<td>
					<a class="btn btn-primary" asp-action="Detalhes" asp-route-id="@item.Nr">Detalhes</a>
					<a class="btn btn-warning" asp-action="Editar" asp-route-id="@item.Nr">Editar</a>
					<form asp-action="Cancelar" asp-route-id="@item.Nr" method="post">
						<button class="btn btn-danger" type="submit">Cancelar</button>
					</form>
					<a class="btn btn-danger" asp-action="Excluir" asp-route-id="@item.Nr">Excluir</a>
				</td>
			</tr>
		}
		</tbody>
	</table>
}
else
{
	<p>Nenhum agendamento cadastrado.</p>
}
